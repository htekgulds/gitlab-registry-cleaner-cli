{
  "name": "gitlab-registry-cleaner-cli",
  "version": "0.1.0",
  "description": "Gitlab Registry Cleaner CLI",
  "bin": {
    "grc": "dist/grc.js"
  },
  "scripts": {
    "build": "esbuild bin/grc.js --platform=node --bundle --outdir=dist --minify",
    "watch": "npm run build -- --watch",
    "lint": "standard"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@clack/prompts": "^0.7.0",
    "axios": "^1.6.2",
    "boxen": "^7.1.1",
    "chalk": "^5.3.0",
    "dotenv-flow": "^4.0.1",
    "overwatch-api": "^0.13.0",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.7",
    "standard": "^17.1.0"
  }
}
